#!../runscript

LOG "You need: dhcpd, tftp-hpa"
PAUSE "Ok?"

sudo apt-get install dhcpd tftp-hpa

LOG "You want to look at our corvix /etc/dhcpd.conf, /etc/tftp"
PAUSE "Ok?"

LOG "Copying files"
[[ -e /srv/tftp ]] || mkdir -p /srv/tftp
cp tmp/isoimage/* /srv/tftp -r
